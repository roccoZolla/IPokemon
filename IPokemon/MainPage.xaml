<Page
    x:Class="IPokemon.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IPokemon"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <Grid>
        <Grid x:Name="grid" Width="1500" Height="1000">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button x:Name="myButton" Content="Accedi al pokedex" Click="myButton_Click" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,200,0,10"/>
            <Button x:Name="btnSecondo" Content="Info" Click="btnSecondo_Click" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,290,0,10"/>
            
        </Grid>

        <Frame x:Name="MainFrame" />
    </Grid>






</Page>
